# Tech Debt

Scored per `engineering:tech-debt` convention: **Priority = (Impact + Risk) × (6 − Effort)**, each dimension 1–5. Higher priority = fix sooner. Nothing here is code debt yet, since no code has shipped — these are debts already implied by Phase 0 decisions, logged now so they aren't rediscovered as surprises later.

| # | Item | Category | Impact | Risk | Effort | Priority | Notes |
|---|---|---|---|---|---|---|---|
| 9 | Broader live-server verification still pending | Test | 3 | 3 | 3 | **18** | Downgraded 2026-07-20: sign-up, checkout, webhook signature handling, and the auth-guard design all independently verified live. Not yet exercised: most CRUD endpoints across Phases 2-6, migrations against a real schema, the seed script itself. |
| 11 | RAG pipeline not connected — `Embedding` table has no writer | Architecture | 3 | 2 | 4 | **15** | Phase 4's search is keyword-only (Postgres FTS, real and working). Phase 6's `apps/ai-service` has no endpoint that generates embeddings and writes to `Embedding`, and no retrieval step feeds them into `/chat`. Both halves exist independently; the RAG wiring between them doesn't. |
| 12 | Better Auth's generated schema not reconciled against hand-authored tables | Architecture | 3 | 3 | 2 | **18** | Same as ADR-002 Action Item 1, now more urgent — User/Session/Account/Verification/Organization/Member/Invitation were hand-authored from documented conventions, never diffed against real `npx @better-auth/cli generate` output. |
| 13 | Stripe billing: webhook events beyond checkout.session.completed unverified | Test | 2 | 2 | 2 | **8** | Downgraded 2026-07-20: checkout session creation verified end-to-end in test mode. `customer.subscription.updated`/`.deleted` handling still unexercised live. Tax calculation is still a flat `0` placeholder, not jurisdiction-aware. |
| 14 | "org:manage_members" permission key reused for workspace-level member management | Documentation | 1 | 1 | 1 | **8** | Cosmetic naming collision inherited from the §4 matrix — functionally correct (Owner/Admin of a workspace can manage that workspace's members) but the key name suggests org scope when it's checked via workspace-scoped PermissionGuard. Rename to `workspace:manage_members` if it causes confusion. |
| 15 | Postgres NULL-uniqueness nuance on system Role rows | Architecture | 2 | 2 | 2 | **12** | `Role.organizationId` is nullable for system roles (Owner/Admin/Member/Guest); Postgres doesn't enforce uniqueness across multiple NULLs in the `[organizationId, name]` composite unique index. Seed script is the only writer today, so this is safe in practice — would need a non-null sentinel value if a second writer of system roles is ever introduced. |
| 16 | No frontend auth flow, no real pages beyond one dashboard shell | Architecture | 4 | 2 | 5 | **18** | `apps/web` is Milestone 1.3's shell requirement, not a product. No Better Auth client SDK integration, no per-module pages, no data fetching. This is the natural next full session's focus. |
| 17 | No streaming for `/chat` | Architecture | 2 | 1 | 3 | **9** | Single-turn request/response only. A real assistant UX wants token streaming; FastAPI/Anthropic both support it, just not implemented this pass. |
| 18 | Goals/OKRs and Notifications are schema-only | Architecture | 2 | 1 | 3 | **9** | Both modeled in `prisma/schema.prisma` (Phase 2) with zero endpoints. Lowest-priority gap in the Work Management module — time tracking and the Kanban/comments/attachments core all have real endpoints. |
| 19 | AI Layer only implements one provider | Architecture | 1 | 1 | 3 | **6** | `AIProvider` abstraction is real and swappable (docs/PHASE_0_ARCHITECTURE.md's AI Layer Design requirement), but only `AnthropicProvider` exists — the abstraction is unproven against a second implementation. |
| 7 | Pinned to TypeScript 6.0.3 instead of 7.0 | Dependency | 2 | 1 | 1 | **15** | Deliberate: TS7 (GA July 8, 2026) broke `typescript-eslint` and `ts-jest`, both used here — fix ships in TS 7.1, not yet released. Revisit once `typescript-eslint`/`ts-jest` confirm 7.1 compatibility. Low impact/risk since 6.0.3 is fully supported, not EOL. |
| 5 | `Guest` role and client-portal permissions exist in the Phase 0 matrix, but the portal UI itself doesn't ship until Phase 3 | Documentation | 2 | 2 | 1 | **20** | Low effort to fix: just make sure Phase 3 planning explicitly checks the matrix instead of re-deriving Guest permissions from scratch. |
| 6 | `Comment`/`Attachment` polymorphic association enforced at the application layer, not the database | Architecture | 2 | 2 | 2 | **16** | Documented limitation, not a bug (see `prisma/schema.prisma` comment). Revisit only if orphaned `entityId` rows actually show up in practice — fix would be splitting into `TaskComment`/`DealComment`. |
| 4 | No CQRS anywhere, including the future Analytics read paths | Architecture | 1 | 1 | 4 | **4** | Intentional per the architecture principles ("CQRS only where read/write patterns genuinely diverge"). Revisit if/when Phase 9 Analytics queries start measurably straining the write-optimized schema. |
| 20 | API keys are org-admin-equivalent, no per-key scoping | Architecture | 3 | 3 | 3 | **18** | `ApiKey.scopes Json?` field exists in the schema but is unused — every key currently grants access to every workspace in its organization. Real scoping (specific workspaces, specific permissions) is a schema-compatible addition whenever it's actually needed. |
| 21 | Workflow conditions are flat equality only | Architecture | 2 | 1 | 3 | **9** | `AutomationProcessor.matchesConditions` checks exact key/value matches against event metadata — no AND/OR trees, no `>`/`<`/`contains` operators. Fine for "trigger when stage becomes X"; not enough for anything more expressive. |
| 22 | `SEND_WEBHOOK` workflow action has no retry | Architecture | 1 | 1 | 2 | **4** | Intentionally distinct from `WebhookSubscription` deliveries (which do retry via BullMQ) — a workflow-configured one-off webhook call is fire-and-forget. Route through `WEBHOOK_DELIVERIES_QUEUE` instead if this needs reliability guarantees. |
| 23 | Plugin system foundation not built | Architecture | 1 | 1 | 5 | **2** | Last item in Phase 7's module list, not reached this pass. The original roadmap itself frames this as "foundation... marketplace (Phase 10+)" — lowest priority in this file by design, not oversight. |
| 24 | Redis pub/sub channel subscriptions never torn down | Architecture | 1 | 1 | 2 | **4** | `RealtimeService.observe()` subscribes on first use and never unsubscribes, even after every SSE client for that channel disconnects. Cheap at current scale (a subscribed-but-unread Redis channel costs almost nothing); revisit only if the number of distinct workspace/user channel names over a long process lifetime becomes large enough to matter. |
| 25 | No unread-count endpoint despite the schema supporting it | Architecture | 1 | 1 | 1 | **6** | `ChannelMember.lastReadAt` exists specifically for this; no controller reads or writes it yet. Cheap, low-risk addition whenever the frontend needs it. |
| 26 | Video room creation not implemented | Architecture | 2 | 1 | 2 | **6** | `docs/adr/005-video-integration.md` picked Daily.co and defined the integration point (`Channel.videoRoomUrl`); the actual endpoint calling Daily's API doesn't exist yet. See the ADR's own action items. |
| 27 | `hasResourcePermission` (advanced permissions) not consumed anywhere | Architecture | 2 | 1 | 3 | **6** | The check exists in `PermissionsService`, the schema (`ResourcePermissionOverride`) is real, the grant/revoke endpoints work — but no existing controller's `@RequirePermission` flow actually calls the resource-level check instead of the plain workspace-level one. It's available, not wired in. |
| 28 | Per-organization rate limiting missing, only per-IP | Architecture | 3 | 2 | 3 | **15** | `docs/PHASE_0_ARCHITECTURE.md`'s Security section called for both "per org and per IP" from Phase 0 planning. `@nestjs/throttler`'s default tracker is IP-only; per-org needs a custom tracker keyed by the resolved session/API key's organization. |
| 29 | Real MIME-type allowlist missing on file uploads | Architecture | 3 | 2 | 1 | **15** | Found during the Phase 10 security review (`SECURITY_REVIEW.md` #1). `AttachmentsController` and `FilesController` both cap size but accept any file type. Cheap fix, flagged as launch-blocking in `LAUNCH_CHECKLIST.md`. |
| 30 | SCIM undecided, not just unimplemented | Documentation | 2 | 1 | 1 | **8** | Unlike every other ADR in this repo, ADR-006 explicitly does NOT pick an implementation path for SCIM — Better Auth's marketing copy claims it, but this session didn't independently verify a concrete package the way SSO's `@better-auth/sso` was verified. Confirm before promising SCIM to anyone. |
| 31 | Marketplace/plugin system is a registry, not a runtime | Architecture | 1 | 1 | 5 | **2** | `IntegrationDefinition`/`OrganizationIntegration` (Phase 10) track what integrations exist and which orgs installed them — there's no OAuth flow, no per-integration webhook receiver, no sandboxed plugin execution. Genuinely the "foundation" the original roadmap asked for, not the feature. Supersedes item #23's framing (same underlying gap, now with an actual schema to build on). |
| 32 | Railway build/start commands live in the dashboard, not committed to the repo | Infrastructure | 2 | 3 | 2 | **10** | Deliberate, not an oversight — see `docs/DEPLOYMENT.md`'s note on a documented, current Railway community-reported failure mode where a committed `railway.json` loses workspace context specifically on GitHub-triggered deploys of shared monorepos. Revisit by testing a GitHub-triggered deploy against a committed config before switching away from dashboard settings. |

## Resolved

| # | Item | Resolution |
|---|---|---|
| 2 | Postgres hosting vendor (Supabase vs. Railway) not finalized | Confirmed Supabase, 2026-07-18. `docs/adr/004-hosting-split.md`'s rationale was corrected in the same pass — `pgvector` is not Supabase-exclusive; the real reason is one-vendor ergonomics with Storage. |
| 10 | Better Auth route-mounting/global-prefix-exclusion unverified live | Verified 2026-07-20 against a real running instance — `POST /api/auth/sign-up/email` resolves correctly. The `{ path: "auth/*path", method: ALL }` exclusion pattern works as written. |
| 1 | Domain events are in-process (`EventEmitter2`), not durable | Fixed 2026-07-21 (Phase 7) exactly as planned in the original note: `AutomationTriggerListener` relays every domain event onto a BullMQ queue, so automation processing survives a worker restart. `ActivityListener` still writes straight to Postgres in-process — that's fine, an audit-log write is lower-stakes than a triggered automation. |

## How to use this file going forward

Every phase should add to this table, not just Phase 0. When a module ships with a known shortcut (a TODO left in code, a test skipped, a migration that's technically reversible but ugly), it gets a row here with the same scoring — not a comment buried in the code that nobody revisits.
